# .deb packaging — planejado, não implementado

Ver `packaging/arch/PKGBUILD` para o empacotamento atual (Arch/Manjaro). Um
pacote `.deb` (`debian/control`, `debian/rules` etc.) fica para uma sprint
futura — a máquina de desenvolvimento deste projeto é Arch-based, sem `apt`
para testar uma instalação `.deb` real.
